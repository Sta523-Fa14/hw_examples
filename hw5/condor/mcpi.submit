universe     = vanilla
executable   = /usr/bin/R
input        = mcpi.R
output       = mcpi_out_$(Process)
log          = mcpi_log_$(Process)
arguments    = --slave
requirements = OpSys == "LINUX" && Arch == "x86_64"
initialdir   = results 
    
queue 200